networks:
  slordefweb:

services:
    web:
        build:
          context: .
          dockerfile: docker/Dockerfile
        restart: unless-stopped
        env_file:
          - .env
        networks:
          - slordefweb
